---
import Concert from "../../components/concerts/Concert.astro";
import { getConcert, getConcerts, getItemsStaticPaths } from "../../helpers";
import Layout from "../../layouts/Layout.astro";

export function getStaticPaths() {
	return getItemsStaticPaths(getConcerts());
}

const { id } = Astro.params;
const concert = getConcert(id);

const { date, titre, commentaire } = concert;
---

<Layout title={titre}>
	<Concert id={id} />
</Layout>
