---
import Actualite from "../../components/actualites/Actualite.compact.astro";
import PageLayout from "../../components/layouts/PageLayout.astro";
import {
	getActualites,
	getConcerts,
	getString,
	SectionId,
} from "../../helpers";
import sortBy from "lodash/sortBy";
import Layout from "../../layouts/Layout.astro";
import Concert from "../../components/concerts/Concert.compact.astro";

const actualites = getActualites();
---

<Layout title={getString("actualites")?.t}>
	<PageLayout sectionId="actualites">
		{
			actualites.map(({ id, sectionId }) => (
				<div slot="page-content">
					<Actualite id={id} />
				</div>
			))
		}
	</PageLayout>
</Layout>
